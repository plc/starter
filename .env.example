# Environment variables for local development
#
# Copy this file to .env and modify as needed:
#   cp .env.example .env
#
# Note: .env is gitignored and should never be committed

# Server port (generate deterministic port with: ./scripts/get-port.sh PROJECT_NAME)
PORT=3000

# PostgreSQL connection string
# Uses host.docker.internal to connect from Docker to host PostgreSQL
DATABASE_URL=postgres://postgres:postgres@host.docker.internal:5432/myapp

# Database name
DB_NAME=myapp
